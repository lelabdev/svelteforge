// Stub of `$app/environment` for repo-root tests: template components (e.g.
// Table's dev-time rowKey warning) import `dev` without pulling in SvelteKit.
// `true` so the dev-only warning paths are exercised under test.
export const dev = true;
export const building = false;
export const version = '0.0.0';
