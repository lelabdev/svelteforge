# eslint-plugin-svforge

Deterministic SvelteForge design-system diagnostics for ESLint.

Enable `svforge/no-design-violations` in a flat ESLint configuration. The rule
shares its identifiers and messages with `svforge check`.
